{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",

  "ConnectionStrings": {
    "DefaultConnection": "{AUTHENTICATIONAPI_CONNECTION_STRING}"
  },

  "ApiSettings": {
      "Secret": "${{ secrets.APISETTINGS_SECRET }}",
      "Issuer": "${{ secrets.APISETTINGS_ISSUER }}",
      "Audience": "${{ secrets.APISETTINGS_AUDIENCE }}"
  }
}
